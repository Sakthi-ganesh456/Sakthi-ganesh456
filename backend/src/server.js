import express from 'express'; import cors from 'cors'; import 'dotenv/config'; import auth from './routes/auth.js'; import market from './routes/market.js'; import seller from './routes/seller.js'; import investments from './routes/investments.js'; import {notFound,errors} from './middleware/errors.js';
const app=express(); app.use(cors({origin:process.env.CORS_ORIGIN?.split(',')}));app.use(express.json({limit:'100kb'}));app.get('/health',(_,res)=>res.json({status:'ok'}));app.use('/api/auth',auth);app.use('/api/market',market);app.use('/api/seller',seller);app.use('/api/investments',investments);app.use(notFound);app.use(errors);app.listen(process.env.PORT||4000,()=>console.log('API running'));
